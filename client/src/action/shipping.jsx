import * as Type from "../contants/Actiontype"
// import { NotificationError, NotificationSuccess } from "../common/Notification";
export const ActConfirmOrder = () => {
    return {
        type: Type.SHIPPING,
    }
}